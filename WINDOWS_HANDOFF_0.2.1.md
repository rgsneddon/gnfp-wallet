# Windows pointer — gnfp-wallet 0.2.1

**Do not use this file as the pin list.** All leftover lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md**

Current pin here: **0.2.1**. Do **not** rebuild **0.2.0**. Desktop Mine start runs bundled **gnfp-cminer 1.1.0** (same 5% miner dual-login fee). Phones keep the Dart hasher. Do **not** rebuild C-miner PE on public `v1.1.0`. `HASH_TX_LIVE` stays **0**.

Attach to **the same** `v0.2.1` tag. No sibling tags.

| File | What is inside |
|------|----------------|
| `gnfp-wallet-0.2.1-windows.zip` | Windows Flutter desktop + `gnfp-cminer.exe` next to the GUI |
| `gnfp-wallet-0.2.1-linux.zip` | Linux Flutter desktop + `gnfp-cminer` |
| `gnfp-wallet-0.2.1-archlinux.zip` | Arch PKGBUILD / same pin |

Title `$GNFP core wallet v0.2.1`. Mac leftover for this pin is **none** once Darwin/Android/iOS are on the tag.
