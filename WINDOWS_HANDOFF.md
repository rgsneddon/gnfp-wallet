# Windows pointer — gnfp-wallet

**Do not use this file as the pin list.** All GNFP leftover lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md**

Current wallet / miner / node pins, what the Mac already shipped, and any laptop leftover are only in that file. **Every wallet pin includes the CLI at that same pin** (`pack\gnfp-cli.cmd --version` must match the GUI). One pin → one GitHub tag. Do not invent a sibling CLI tag. Do not rebuild pins that file marks **on tag**.
